# Core dependencies (fallback for pip users)
pufferlib>=1.0.0
playwright>=1.40.0
gymnasium>=0.29.0
torch>=2.0.0
numpy>=1.24.0,<2.0.0
pillow>=10.0.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
ruff>=0.1.0

# Logging (optional)
wandb>=0.16.0
